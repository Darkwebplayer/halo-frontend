<?xml version="1.0" encoding="utf-8"?>
<!-- Flat fill in the artwork's own background colour, so the tile inset into the foreground
     layer meets it without a visible seam whatever mask the launcher applies. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#EDF1FC"
        android:pathData="M0,0h108v108h-108z" />
</vector>
